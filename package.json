{
  "name": "claude-sub-proxy",
  "version": "0.1.0",
  "description": "OpenAI-compatible proxy that fronts the Claude Agent SDK with caller-side function-call passthrough, billing your Claude subscription.",
  "type": "module",
  "scripts": {
    "build": "nitro build",
    "dev": "nitro dev",
    "start": "node .output/server/index.mjs",
    "smoke": "node smoke.mjs"
  },
  "license": "MIT",
  "packageManager": "pnpm@10.32.1",
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.1.18",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "nitropack": "^2.10.0",
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  }
}
